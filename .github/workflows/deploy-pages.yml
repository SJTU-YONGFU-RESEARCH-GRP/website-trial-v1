# GitHub Pages: build Vite app at repo root and deploy dist/
# Repo: https://github.com/SJTU-YONGFU-RESEARCH-GRP/website-trial-v1
#
# Site URL: https://sjtu-yongfu-research-grp.github.io/website-trial-v1/
#
# Setup (once): Settings → Pages → Build and deployment → Source: GitHub Actions

name: Deploy to GitHub Pages

on:
  push:
    branches: [main, master]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # Optional: add npm cache after package-lock.json is stable in git:
      #   with:
      #     cache: npm
      #     cache-dependency-path: package-lock.json
      - uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install and build
        run: |
          npm ci
          npm run build

      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
